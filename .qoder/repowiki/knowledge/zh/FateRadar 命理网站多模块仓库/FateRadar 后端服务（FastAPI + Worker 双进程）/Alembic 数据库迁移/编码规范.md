- 每个迁移文件顶部包含 docstring 形式的 Revision ID、Revises、Create Date，并在模块级声明 `revision`、`down_revision`、`branch_labels`、`depends_on` 四个 Alembic 元数据变量。
- 表结构变更统一通过 `alembic.op` 的 `create_table` / `drop_table` / `create_index` 等 DDL 函数完成，而非原生 SQL。
- 外键、主键、唯一约束和索引均显式命名，遵循 `pk_`、`fk_*`、`uq_*`、`ix_*` 前缀约定，便于 downgrade 精确回滚。
- 时间戳字段统一使用 `sa.DateTime(timezone=True)` 并配合 `server_default=sa.text("CURRENT_TIMESTAMP")` 设置默认值。
- 迁移按语义化编号递增（0001_identity_foundation → 0008_admin_staff_foundation），且 `down_revision` 指向上一版本号形成线性链。