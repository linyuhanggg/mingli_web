Next.js App Router（Route Handlers 风格的 `metadata`、`dynamic`、`revalidate`、`fetchCache` 导出），React Server Components + 少量 client 组件（如 `app/error.tsx` 标注 `"use client"`），PWA 通过 `manifest.ts` 生成 Web App Manifest。