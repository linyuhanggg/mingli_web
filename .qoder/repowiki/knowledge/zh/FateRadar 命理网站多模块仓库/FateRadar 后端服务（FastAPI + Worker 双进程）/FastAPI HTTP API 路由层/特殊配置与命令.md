无独立构建命令；需确保应用启动时将 `session_factory`、`settings`、`otp_challenge_store`、`otp_delivery`、`otp_code_factory`、`otp_request_limiter`、`trusted_proxy_networks`、`profile_write_rate_limiter`、`admin_login_rate_limiter` 等对象挂到 `request.app.state`，否则依赖注入会失败。