发布证据位于 `docs/releases/evidence/`，其中 Task13 轨迹通过本地 runner 脚本在测试服环境（local + fake OTP + Runtime one-shot + Model deepseek）下重放并产出 `run-summary.json`、`console.log` 等可审计产物；交接快照 `HANDOFF_SNAPSHOT_*` 明确当前代码断点（如 `main @ 0525eb1`）与测试服务器地址 `fateradar-prod`，供后续会话直接复用。