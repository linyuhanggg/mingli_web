测试按被测领域横向切分：`conftest.py` 提供共享 fixture——以 `sqlite+aiosqlite:///:memory:` 动态创建数据库、注册所有 domain model（identity/profiles/readings/admin），并通过 `app.main.create_app` 组装测试用 FastAPI 应用，暴露 `client`、`database`、`test_settings` 三个顶层 fixture；`orchestrator_fakes.py` 集中实现编排器测试的假依赖：`ScriptedRuntime`/`ScriptedModel`/`ScriptedGuard`/`MemoryRepository`/`FixedClock` 配合 `make_job`/`make_prepare`/`make_candidate`/`make_prepared` 工厂，使 `test_reading_orchestrator_*` 系列能精确控制 runtime 行为并断言 checkpoint 状态机流转；`fixtures/mingli/` 与 `fixtures/narrative/` 存放 JSON 格式的 prepare payload 与 narrative candidate，被各编排器测试复用；其余 `test_*.py` 文件按功能域组织（auth、admin_auth、guest_sessions、readings_api、profiles_api、network、openapi_alignment、migrations、worker、runtime_*、narrative_guard 等），均通过 `client` fixture 发起 HTTP 请求或直调模块函数，形成“HTTP 集成测试 + 纯函数/类单元测试”的双层结构。依赖方向单向指向被测 `app.*` 模块，测试代码不反向被生产引用。