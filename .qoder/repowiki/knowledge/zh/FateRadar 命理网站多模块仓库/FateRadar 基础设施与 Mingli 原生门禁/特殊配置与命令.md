- 本地开发：`docker compose -f infra/compose.local.yml up` 启动 postgres/redis/api/worker/web/edge，默认监听 127.0.0.1:8080(nginx)/3000(web)/8000(api)/5432(postgres)/6379(redis)。
- 原生门禁唯一命令：`uv run --project backend python infra/mingli-runtime/local_gate.py native-full --prepared-inputs <path> --prepared-inputs-sha256 <sha256> --output-parent <empty-dir>`，产出 `native-full-5.1.json`、`local-native-full-5.1.json`、`native-release-regression.{stdout,stderr}`、`prepared-inputs.json`。
- Task 13 轨迹：`OUT_DIR=/tmp/task13-server-trajectory API=http://127.0.0.1:8000/api/v1 bash scripts/run_server_task13_trajectory.sh`，需先启动本地 compose 环境并使用 fake OTP。
- 构建上下文：`python infra/mingli-runtime/build_context.py --source-root <signed-release> --destination <new-empty-dir>` 生成受 manifest 约束的 Docker build context。