Python 异步编排，SQLAlchemy AsyncSession + PostgreSQL（`with_for_update` 行锁序列化版本分配），JSON Schema Draft202012（`jsonschema`）校验 mingli-command/result 协议，`dataclasses(frozen=True, slots=True)` 构建不可变值对象，Protocol 定义外部端口。