- 页面按 Next.js App Router 约定组织在 `src/app/<route>/page.tsx`，并通过同目录 `layout.tsx` 定义子路由壳层
- 每个组件与其样式文件同名共存（如 `xxx.tsx` 对应 `xxx.module.css`），使用 CSS Modules 隔离样式
- 跨页面共享逻辑统一放入 `src/lib/` 并以纯函数或类形式暴露，避免在页面中直接实现业务细节
- 测试文件集中放在 `src/test/`，按被测目标命名（如 `api-readings.test.ts`、`reading-result.test.tsx`）