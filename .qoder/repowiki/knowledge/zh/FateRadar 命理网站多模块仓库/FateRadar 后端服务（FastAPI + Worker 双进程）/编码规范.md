- 每个领域子模块统一采用 models/repository/service/schemas 四层划分，对外仅暴露 service 与 schemas
- 外部依赖（模型推理、短信 OTP、支付、运行时沙箱）均通过 adapters 包中的协议化适配层访问，并提供 Fake/Disabled 实现用于测试与降级
- HTTP API 路由集中在 app/api 下按资源拆分子文件，并通过 dependencies.py 统一装配鉴权、CSRF、限流等横切逻辑
- 数据库变更统一通过 Alembic 迁移脚本演进，禁止直接修改已发布版本的 schema