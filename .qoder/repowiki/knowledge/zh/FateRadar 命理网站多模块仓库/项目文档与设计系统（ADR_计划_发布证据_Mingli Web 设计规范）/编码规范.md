- ADR 采用四位数字前缀加语义化标题的文件命名（如 `0001-use-a-modular-monolith-on-alibaba-cloud.md`），并按序号递增记录决策历史。
- 设计系统遵循「Master 总则 + 页面级 override」的分层约定：构建具体页面时优先检查 `design-system/mingli-web/pages/[page-name].md`，存在则覆盖 Master，否则沿用全局规则。
- 设计令牌（颜色、间距、动效）以 CSS 变量形式声明（如 `--ink-950`、`--space-md`、`--ease-out-expo`），并在 MASTER 中集中维护，禁止随意引入新库或主题。
- 发布证据按日期+任务维度归档到 `docs/releases/evidence/<date>/<task>/`，每个 run 保留结构化摘要（`*.json`）与控制台输出（`*.log`），保证结果可复现。
- 交接与阶段门禁文档（`HANDOFF_SNAPSHOT_*`、`PHASE_0_GATES.md`）以“已完成/未闭环/下一步/关键事实与禁止项”四段式结构组织，明确当前断点与放行条件。