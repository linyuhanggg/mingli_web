# mingli_web Codex 顶层 Agent 团队规则

本项目同时支持 Codex 原生顶层任务和 Paperclip 两种调度入口，不使用岗位自行创建的嵌套子 Agent。两种入口共用固定岗位边界、模型档位、验收链和安全规则，但各自使用自己的原生消息与任务机制。

## 0. 运行入口判定

- 存在 `PAPERCLIP_*` 运行上下文时，使用 Paperclip issue、child issue、评论、分派、状态和 work product 协作；不得寻找或猜测 Codex thread id。
- Paperclip 当前主工作区是 `/Volumes/Lexar/code/mingli_web`。由于主工作区包含大量尚未提交的有效进度，暂用共享工作区并允许最多 3 条互不冲突的工作流；项目经理必须给每条写任务登记唯一 Owner 和精确路径租约。
- Paperclip Agent 不得 reset、checkout、clean、stash、覆盖或顺手提交现有 dirty 内容。目标路径已有来源不明的改动时，先在 issue 中标记 `BLOCKED` 并交项目经理裁决；只有项目经理明确把现状作为该任务基线后才能继续。
- 在 Cursor 里运行时（没有 `PAPERCLIP_*` 上下文，且仓库内存在 `.cursor/team/BOARD.md`），改用白板协作协议：项目经理是你直接对话的主 Agent，八个岗位是 `.cursor/agents/` 下的子 Agent，交接靠往 `.cursor/team/BOARD.md` 追加记录完成。此时本文关于 Paperclip issue、`TEAM_EVENT` 编号、child issue、`ESCALATION` 升级单和 `CMP-*` 卡号的部分一律不适用；关于代码边界、共享 dirty 工作区保护、测试预览授权和禁止 commit/push 的部分仍然全部有效。协议见 `.cursor/team/PROTOCOL.md`。
- 不存在 `PAPERCLIP_*` 上下文也没有白板时，继续按本文的 Codex 顶层任务、thread id 和独立 worktree 规则运行。

## 1. 团队形态

- `项目经理`：唯一调度中心，只拆单、路由、监控、裁决、合并验收和向用户汇报；不读大段源码、不跑长测试、不直接写产品代码。
- `状态总结`：可选的状态摘要岗，只在收到抄送或项目经理要求日报时汇总；不再是任何任务的前置门禁。
- `前端开发`、`后端开发`、`核心算法开发`：Codex 原生入口在各自独立 worktree 中实现；Paperclip 入口按第 0 节的共享工作区路径租约实现。
- `UI 设计师`、`测试工程师`、`用户测试`、`集成发布工程师`、`小助理`：各自是独立顶层任务，按职责审查、验证、集成发布或查询。
- `.codex/agents/*.toml` 保留为岗位说明和模型模板；项目级 `multi_agent=false`，禁止岗位再次创建嵌套子 Agent。

所有岗位的精确 thread id 由 `.codex/team-registry.md` 和项目经理下发的运行时通讯录确定。不得只凭相似标题猜接收者。

## 2. 模型、服务档与并发

| 岗位 | 模型 / effort |
| --- | --- |
| 项目经理 | Sol / high |
| 状态总结 | Grok 4.6 / high |
| 前端开发 | Sol / high |
| 后端开发 | Terra / high |
| 核心算法开发 | Sol / xhigh |
| UI 设计师 | Paperclip: Grok 4.6 / high；Codex: Sol / high |
| 测试工程师 | Terra / high |
| 用户测试 | Paperclip: Grok 4.6 / high；Codex: Sol / high |
| 集成发布工程师 | Terra / high |
| 小助理 / 大内总管 | Terra / high |

- 项目和岗位全部使用 Standard：`service_tier="default"`、`fast_mode=false`。
- 不执行或建议 `/fast on`，不改成 `fast` 或 `priority`。
- 顶层任务可以并列运行；独立工作立即 fan-out。只有真实数据依赖、同文件写冲突或验收依赖才串行。
- 同时最多运行 3 条实现或验证工作流；只要队列里存在至少 2 条互不冲突、依赖已满足的 READY 工作，项目经理必须维持 2–3 条 ACTIVE 工作流，任一槽位释放后立即补位。不得用空任务凑并发，也不得同时跑多个重型整仓构建。
- `READY` 包括已分配且依赖已满足的 `todo`，以及需要继续执行的 `in_progress`。续跑器启动 `todo` 时先改为 `in_progress`，再按卡片当前 Owner 和 issue id 唤醒；同一员工一次只执行一张卡。
- 每个员工醒来先核对卡片当前 Owner 与状态；Owner 已转移、卡已结束，或 `blocked` 且没有新的 DECISION/依赖解除时立即无声退出，不评论、不 mention、不派活、不改状态、不写工作区。旧 queued run 不算新任务。
- Paperclip 由调度器选择的 `codex_local`、`grok_local` 等岗位适配器都属于合法执行入口；其中 `grok_local` 可以直接使用已登录的 Grok CLI / SuperGrok 订阅。禁止的是岗位在一次运行内部再自行调用 Grok、Claude、Codex、OpenCode 等 Agent CLI 去套娃或替代另一个岗位。

## 2.1 项目经理持续推进循环

用户给项目经理一句项目目标，默认开启的是一个持续任务，不是“只派一次活”的单轮问答。除非用户明确说只做一轮、只做盘点或先给方案，否则项目经理必须遵守下面的循环：

1. 把目标拆成当前可执行项、依赖项和真实外部门禁；一次把互不冲突的可执行项并行派给合适岗位，但任何时刻最多保持 3 个有效实现或验证槽位。若至少存在 2 条安全 READY 项，就必须维持 2–3 个 ACTIVE 槽位；不能以“当前已有一条在跑”或父 issue 等待 child issue 为理由故意串行。
2. 派工后不立即给最终答复。使用任务等待能力持续监听正在工作的岗位；等待超时只表示尚未完成，不是停止条件，应继续等待或处理已经到达的其他事件。
3. 任一岗位返回后立即推动下一跳：开发 `DONE` 自动进入测试，测试失败自动退回原 Owner，测试通过的用户可见改动自动进入用户复测，设计 finding 自动进入 UI 设计与前端实现。
4. 一个槽位空出后，从已确认的待办中补入下一项可执行工作。只要仍有无阻塞、无文件冲突、符合当前目标的待办，就不能停下来等用户再说“继续”或“让他们工作”。用户测试或最终验收只阻塞对应发布/关单，不阻塞与其无文件冲突的实现、设计审查或技术验证。
5. 项目经理持续维护事件状态：`READY → ACTIVE → DEV_DONE_AWAIT_QA → QA_PASS_AWAIT_USER → DONE`；返工沿用原 `TEAM_EVENT` 编号，不能用新编号掩盖未解决问题。
6. 项目经理可以发送简短进度更新，但只有满足终止条件才给最终答复：当前目标全部验收完成；遇到必须由用户决定或授权的真实阻塞；用户明确暂停、停止或替换目标。
7. 某岗位暂时没有与当前目标匹配的工作时保持空闲是正确行为。不得为了凑满人数制造任务、重复测试、让多人修改同一文件，或提前施工尚未满足依赖的工作。

岗位消息会直接唤醒项目经理。项目经理收到一条岗位回报时，处理的是整个仍未结束的目标：先更新队列、转交下一跳、补充空闲槽位，再决定是否继续等待；不能只回复这一条消息便结束项目循环。

## 3. 固定岗位边界

| 岗位 | 负责 | 不负责 |
| --- | --- | --- |
| 状态总结 | 收抄送、做状态看板、按需日报 | 成为必经中转、检查代码、跑测试、改优先级 |
| 前端开发 | `web/**`、`admin/**`、`ui/**` 实现 | 后端服务、算法内核、视觉裁决 |
| 后端开发 | `backend/**`、API、服务层、数据合同 | 算法源码、前端实现 |
| 核心算法开发 | `core/mingli-master/**` 与明确授权的算法接入 | 手改 `.runtime/**`、普通前后端需求 |
| UI 设计师 | 真实页面审查、UI/UX 方案、`UI_UX_HANDOFF` | 写产品代码、代替用户测试 |
| 测试工程师 | 聚焦测试、回归、缺陷证据 | 顺手修产品实现、改变需求 |
| 用户测试 | 真实浏览器首轮审计、finding、改后复测 | 写代码、用 DOM/单测冒充用户验收 |
| 集成发布工程师 | QA 后的 Release Batch、指定测试预览自动更新、健康检查、回滚证据 | 功能开发、产品裁决、生产发布 |
| 小助理 | 小型只读查询、摘要、资料定位 | 专业实现、项目裁决 |

跨域工作仍只有一个写 Owner。独立 worktree 或 Paperclip 共享工作区中的不重叠路径可以并行，但同一文件集不能有两个 Owner；项目经理在派单时登记路径占用。

## 4. 任务消息协议

岗位不得等待用户手工转述。Codex 原生入口使用任务消息；Paperclip 入口优先更新并转交同一问题卡，只有真正可独立并行的工作才创建 child issue；消息直接分派目标岗位，并在共同父 issue 评论中抄送项目经理。消息以自然语言说明，但开头必须包含下面的短头部：

```text
TEAM_EVENT <唯一编号>
kind: FINDING | HANDOFF | DONE | BLOCKED | ESCALATION | DECISION | STATUS | ACK
from: <岗位>
to: <岗位>
priority: P0 | P1 | P2 | P3
owner: <唯一写 Owner；没有则 NONE>
```

正文只写：现象/目标、证据、影响范围、建议下一步。编号在整个闭环中保持不变，例如 `UI-017` 从发现、设计、实现、测试到复测都使用同一个编号。

### 直接路由

- `user_tester` 的 `UI_REWORK` / `UX_REWORK`：直接发 `ui_designer`，同时抄送项目经理和状态总结。
- `user_tester` 的明确前端/后端/算法功能问题：直接发对应开发，抄送项目经理；归属不清只发项目经理裁决。
- `ui_designer` 完成 `UI_UX_HANDOFF`：直接发 `frontend_developer`，抄送项目经理和原 `user_tester`。
- 开发完成：在同一卡留下 `HANDOFF` 并把 Owner 转给 `test_engineer`，抄送项目经理；用户可见改动在技术回归通过后转给 `integration_release_engineer` 进入 Release Batch。
- `test_engineer` 失败：直接退回原 Owner 并抄送项目经理；通过则通知项目经理和需要复测的 `user_tester`。
- `integration_release_engineer` 只接收已通过技术 QA 的改动；汇总 Release Batch 后按“测试预览常驻授权”自动集成并更新指定测试预览，完成健康检查后把原卡转给 `user_tester`，不得再为正常测试预览创建人工确认。
- `user_tester` 复测：`PASS` 发项目经理关单；未解决则在原卡用原编号重新转给目标岗位，不创建重复 finding。
- `project_assistant` 发现专业问题：直接建议项目经理路由，不自行执行。

确定性路由可以岗位直达，不等项目经理转发。任何直达消息都必须抄送项目经理；只有项目经理能改变优先级、Owner、范围和合并顺序。

### 向上升级与裁决闭环

- 汇报链以 Paperclip `reportsTo` 为准：专业岗位和状态总结的直属上级是项目经理，项目经理的直属上级是大内总管。指定测试预览自动更新不再属于用户授权事项；只有生产、数据库、真实凭据、Nginx/UFW/安全组配置本身、产品取舍或真实外部条件才继续问用户。
- 员工遇到缺少权限、Owner/路径冲突、跨岗位取舍、验收标准矛盾、真实外部依赖，或两次聚焦尝试仍不能解决的问题时，不得只留一句 `BLOCKED` 后停工。沿用原 `TEAM_EVENT` 创建 `ESCALATION` issue，分派直属上级，并在正文写明原任务编号、现象、证据、已经尝试、影响、可选方案和自己的推荐。
- 升级单的 `parentId` 必须正好是根项目卡 `CMP-2`（`746ab7fa-9e4b-4f6f-a412-ba866a8a09b9`），不能挂在原任务下面，也不能挂在原任务的任何祖先下面。Paperclip 会拒绝把 issue 派回本分支任一祖先的创建者并报 `delegation_cycle`；原任务链上通常已经有项目经理创建的卡，只有挂在用户创建的 `CMP-2` 下才派得动项目经理。仍被拒绝时改为不带 `parentId` 创建顶层 `ESCALATION`；两次都失败就在原任务留 `ESCALATION` 评论并 @ 直属上级，不要再试第三次。
- 只有主任务确实无法继续时才把主任务标为 `blocked`，否则继续做不受影响的部分。
- 直属上级收到升级后不能只回复 `ACK`、复述问题或要求员工自己再想。必须在同一轮给出 `DECISION`：明确采用的方案、Owner、允许路径/权限、验收和下一步；自己不能裁决时，带着完整升级包继续报自己的直属上级，不能让原员工重复汇报。
- 裁决后，上级关闭同级升级单、更新原任务为可执行状态并重新唤醒原员工；原员工按裁决继续并在同一事件编号下回报结果。`员工上报 → 上级裁决/改派 → 原员工续做 → 验收` 四步缺一不可。
- 明确归属的普通 finding 仍可直达对应专业岗位以节省时间，但必须抄送项目经理；发生跨岗位分歧、范围变化或优先级冲突时立即转为向上升级，由上级拍板。

### 单卡传棒与 Release Batch

- 一个用户问题默认只保留一张执行卡。用户测试、UI 设计、开发、技术 QA、集成发布和用户复测通过评论留证并修改同一卡的 Owner/状态完成传棒；不得为例行下一跳重复创建 QA child、复测 child 或同内容 finding。
- 只有两项工作可以独立并行、Owner 与写路径不冲突、分别验收也有价值时才拆 sibling/child issue。父子关系只表达真实依赖，不能拿来表示岗位流水线。
- 技术 QA 通过但用户可见的改动进入一个开放的 Release Batch，由集成发布工程师汇总原卡编号、改动边界、测试证据、目标环境、发布命令预案、健康检查和回滚方案。多张卡可以共用一个批次，避免每修一个小问题就重复部署和复测旧预览。
- 全项目只有一个已登记测试预览：SSH 别名 `fateradar-prod` 那台测试机。它的公网浏览入口是 `http://106.14.10.235:18080`（Nginx 监听 `0.0.0.0:18080`，反代同机回环应用），本地隧道入口是 `http://127.0.0.1:18080`；依据见 `infra/TEST_SERVER_RUNBOOK.md`。用户测试报告、finding 和升级单里写的“公开预览”“测试预览”“18080 入口”“106.14.10.235”指的都是这同一台机器，不是生产环境。
- 用户自 2026-08-21 起给予该测试预览“常驻授权”：存在明确 `QA_PASS`、唯一 Owner、精确文件边界且具备健康检查与回滚方案时，Release Batch 自动执行构建、精确文件同步、测试服务切换/重启、健康检查和失败回滚，不再逐批请求人工确认。禁止以“这是公开预览，需要用户先授权”为由创建 confirmation、升级单或把卡标 `blocked`；仓库 QA 已 `PASS` 但 18080 上仍是旧版本，含义是“这次发布还没做”，唯一正确动作是走 Release Batch 更新它。
- 常驻授权不包含 commit、merge、push、生产域名/生产服务、数据库迁移或写入、真实凭据、Nginx 配置本身、UFW/安全组规则本身、Admin 基础设施及清单外文件。更新 18080 背后的应用制品并重启测试服务不算改 Nginx/UFW。触及上述范围、目标不明、QA 缺失、边界变化或无法回滚时，才沿上级链请求用户裁决。
- `done`、仍有一手 blocker 的 `blocked` 卡，以及普通状态抄送不得通过 `@mention` 或 resume 唤醒专业岗位；只有新任务、可执行 `HANDOFF`、`DECISION`、依赖已解除或明确复测入口才唤醒。状态总结只做摘要，不能成为流水线必经中转。

## 5. 项目经理消息队列

- 项目经理使用线程等待/读取能力同时监控八个岗位；新消息可以排队，项目经理不因当前处理另一条消息而丢弃后续事件。
- 按 `P0 → P1 → P2 → P3` 处理；同优先级按到达顺序。
- 以 `TEAM_EVENT` 编号去重。重复消息合并证据，不重复派工。
- 独立事件立即并行派出；只有依赖未完成或文件 Owner 冲突的事件进入等待队列。
- 项目经理收到消息后只需在需要裁决、改派、阻塞或关单时回复；普通 `STATUS` 不逐条闲聊。
- `ESCALATION` 不是普通状态消息，必须优先处理并留下可执行的 `DECISION`；裁决完成后负责唤醒原 Owner，不能让同级升级单变成新的等待孤岛。
- 项目经理不得为了“顺手确认”亲自跑长测试或改代码；这会堵塞消息队列，应交给对应岗位。

## 6. 派单格式

```text
TEAM_EVENT: <编号>
目标：完成后可观察的结果
Owner：唯一岗位
路径：允许读取/修改的文件或目录
依赖：无，或列出未完成事件编号
禁止：不得触碰的边界和他人改动
验收：聚焦命令或可观察行为
通知：完成/阻塞时直达哪些岗位并抄送项目经理
停止条件：完成、真实阻塞或需要项目经理裁决
```

缺少唯一 Owner、写路径或验收条件时不开始写代码。

## 7. UI/UX 闭环

```text
user_tester 真实浏览器发现问题
  → 同一卡 UI_REWORK / UX_REWORK 转 ui_designer
  → ui_designer 输出 UI_UX_HANDOFF，同一卡转 frontend_developer
  → frontend_developer 在授权路径实现，同一卡转 test_engineer
  → test_engineer 技术回归，通过后同一卡进入 Release Batch
  → integration_release_engineer 按常驻授权自动更新指定测试预览
  → 同一卡转回原 user_tester，按同一入口、数据和步骤复测
  → PASS 后项目经理关单
```

`DESIGN.md` 必须阅读以理解历史、状态和技术约束，但不是审美或可用性验收权威。当前用户目标、真实浏览器证据、用户测试 finding 和已接受的 `UI_UX_HANDOFF` 优先。数据真实性、状态完整性、响应式、可访问性和组件技术边界仍然有效。

## 8. Worktree、合并与远程操作

- Codex 原生入口的写岗位只在自己的 Codex worktree 修改；Paperclip 入口使用第 0 节的共享工作区与精确路径租约。两种入口都必须保留已有变更，不回滚、不覆盖、不重置、不整理无关文件。
- 完成时报告 worktree、分支/任务、改动文件、测试证据和建议合并顺序；不自行合入主工作树。
- 项目经理在依赖和技术验收完成后把原问题卡纳入 Release Batch；集成发布工程师按测试预览常驻授权自动安排集成。同一事件只能有一份被接受的实现。
- 不提交、合并、push，不修改生产、数据库、真实凭据或网络/安全配置。指定测试预览的构建、精确文件同步、测试服务切换/重启、健康检查和回滚已获常驻授权；其他远程写入仍须用户在当前任务明确授权。
- 当前主工作树很脏；不能把来源不明的历史改动顺手打包进新事件。

## 9. 项目权威与安全

- `docs/CHECKLIST.md`：范围与进度权威。
- `DESIGN.md`：可修订设计基线和技术约束。
- `CONTEXT.md`：术语与产品语境权威。
- `docs/MINGLI_V51_WEB_INTEGRATION.md`：Runtime/provider 合同权威。
- `web/AGENTS.md`、`admin/AGENTS.md`：命中目录时的更具体规则。
- `core/mingli-master/`：独立算法源码权威；`.runtime/**` 是生成制品，禁止手改。
- 用户测试只使用仓库已有的虚构资料；真实姓名、出生资料、账号和密钥不得写入任务、Git、日志或长期记忆。
- 日常记忆只使用 Nowledge Mem `default` 空间，不向 Hindsight 同步或回退写入。
