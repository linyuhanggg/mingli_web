# Deprecated fortune-cron example

This file is retained only so old links do not break. The June 28 workflow used a retired near-time surrogate, repeated-probe convergence, fixed one-line copy, and legacy fields such as `signal_ledger`, `stable_lenses`, and `unsupported_lenses`. None of those rules may authorize a current answer.

Use `references/fortune-cron-reminders.md` and the `fortune-public-v6-mechanism-stack` contract instead. Current requirements are:

- recalculate the full natal chart and active major luck from confirmed birth facts;
- preserve lunar/ganzhi normalization for the target date;
- compile the natal baseline, active luck, target transits, and their explicit dependency groups;
- filter classical rules by hard applicability before ranking;
- use queried-hour facts only when they materially distinguish the requested period;
- generate wording from the current mechanisms and user question, not from a saved sentence;
- validate the exact public copy with the current `fortune-public` gate before delivery.

Do not copy any public wording or output shape from the historical example.
