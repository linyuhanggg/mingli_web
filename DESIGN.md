---
name: 纸墨档案 UI Contract
status: revisable-baseline
frozen_at: 2026-08-13
revised_at: 2026-08-22
revision_basis: 2026-08-21 用户裁决（P4-007 首轮结论：首页勉强可用，其余页面整体不合格；授权废弃方向 C 审美合同重写）; 2026-08-22 用户补钉（页面效果与动效朝 TabTin 完成度改编，不换暗色皮肤）; docs/redesign/2026-08-21-reference-analysis.md; docs/redesign/2026-08-22-tabtin-direction.md; docs/redesign/2026-08-21-bazi-flow-spec.md; 保留原 §19–§22 证据/能力/交互/验收合同与全部技术硬约束
scope: web-and-admin
---

# UI、组件、交互与响应式基线合同

本文是公共 Web、用户区、产品工作台、`/_ui-lab` 与独立 Admin 的设计基线与技术约束合同。

> **2026-08-21 重写说明**：经用户授权，2026-08-14 起的方向 C「现代 SaaS 锐感」审美合同废弃，由本版「纸墨档案」视觉语言取代；废弃与保留的逐条清单见 §23。数据真实性、状态完整性、响应式、可访问性、组件技术边界，以及 §19–§22 的证据层、能力分层、交互质量与差异化验收合同**全部继续有效**。当前用户目标、真实浏览器中的用户测试结论，以及项目经理接受的 `UI_UX_HANDOFF` 可以推翻本文审美细节；新方案验收后回写本文。

**产品差异化命题（决定后续全部视觉与交互取舍）**：参考站卖「算得准」，本产品卖「每一句都能翻回原书第几行」。前者不可验证，后者可当场证伪。§19 规定证据怎么呈现，§20 规定能力不足时页面怎么收缩，§21 规定交互质量，§22 规定如何**可判定地**证明超过参考站。页面范围、状态库存、依赖和进度只看 `docs/CHECKLIST.md`；名词只看 `CONTEXT.md`；盘面和解读数据边界只看 `docs/MINGLI_V51_WEB_INTEGRATION.md`。

## 1. 三层权威与冲突顺序

本产品不是复刻任一参考站，而是固定组合三种职责：

1. 自有 `mingli-master` Runtime 决定算法、盘面事实、古籍证据、Provider 能力与连续状态；
2. 青囊实站审计决定产品层级、功能入口、单术/合参关系和“免费确定性盘面 → 深度解读”的任务模式；
3. METIS 实站与公开 MIT 仓库决定任务流骨架（单页输入、同路由出盘、盘主文辅、时间层）与响应式表现。

冲突时按以下顺序裁决：

1. Runtime 与版本化数据合同；
2. `docs/CHECKLIST.md` 已冻结的产品地图和本期范围；
3. 本文的可访问性、数据真实性和响应式底线；
4. METIS 的任务流参考；
5. 青囊的盘面版式参考。

两个参考站都不决定品牌皮肤、业务、价格、权益或算法。MIT 代码只能作为表现层来源，使用实质代码时保留版权与许可；客户端算法、时间换算、Prompt、账户数据和生产私有逻辑不得带入。

设计证据：

- [青囊登录态产品与响应式审计](./docs/releases/evidence/2026-08-12-reference-site-audits/qingnang-authenticated-product-audit.md)
- [METIS 生产站、响应式与开源边界审计](./docs/releases/evidence/2026-08-12-reference-site-audits/metis-live-responsive-ui-audit.md)
- [2026-08-21 参考站逐点处置](./docs/redesign/2026-08-21-reference-analysis.md)

## 2. 当前视觉方向：纸墨档案（2026-08-21 起）

一句话：**把「可核验的命理档案」做成纸上的东西**。宣纸底、墨字、朱砂唯一强调、金线专属证据、宋体域字盘面。首页已验证这套语言成立（P4-007 首轮唯一勉强可用页），本轮把它从首页例外提升为全站 C 端统一语言，消灭「首页宣纸、产品页白板」的品牌断崖。

### 2.1 设计判读与旋钮

- 判读：产品页族 overhaul、首页 preserve；受众为看得懂干支盘、对可核验性敏感的中文用户；工作台密度跟 METIS（信息先于呼吸感）。
- 旋钮：盘面工作台 VARIANCE 4 / MOTION 2 / DENSITY 7；入口与公共页 VARIANCE 5 / MOTION 3 / DENSITY 5。2026-08-22 完成度补钉：公共页章节揭示可将 MOTION 调到 4（只动 opacity/transform、一次落定）；盘面 MOTION 仍为 2。首页身份（宣纸剧场、主标、双 CTA）保留，允许按 TabTin 节奏做章节编号、字阶对比、册页入画与时序，不重做视觉身份。
- 质感只允许来自：纸面层次、1px 墨线、字阶对比、按柱对齐的网格、克制微交互。禁止用装饰冒充质感（多层软阴影、发光、彩虹状态色、渐变标题）。

### 2.2 核心决定

1. **宣纸底全站 C 端统一**。`--color-canvas` 等基底 Token 改为纸系（§3）。原「宣纸只限首页与录入页」的例外条款废止——不再需要例外，因为它就是基线。首页的**动效装饰层**（水墨呼吸、符箓漂移、液态玻璃顶栏）仍是 `/` 路由级例外，不外溢（§6.3.1、§15）。
2. **墨是主操作色**。主按钮墨底纸字；黑色主操作延续 2026-08-19 裁定。
3. **朱砂是唯一交互强调色**。链接、选中、焦点环、当前项一律朱砂；全站蓝色强调废止。绿/黄/红只保留为表单与订单的成功/警告/危险语义。
4. **金只属于证据**。金线、金底 wash 专用于古籍命中、引文卡、可核验标记（§19.1 的抽屉与徽章）。金不做文字色、不做按钮、不做装饰边框。用户学会「见金=有出处」。
5. **五行域色只染事实**。木青、火赤、土赭、金灰青、水玄五色仅用于盘面内干支、星曜、爻、卦名的类别标识；永不用于按钮、链接、状态、吉凶（§19.3 禁吉凶染色仍有效）。
6. **盘面是产品的脸**。每术入口态右侧放该术**空盘剪影**（结构空格，无假数据），提交后同路由原地进入盘面态；五术靠盘面结构互相区分，不靠换皮（§2.3、§7）。
7. **产品页标题降调**。产品页 H1 用 20–24px 术名 + 一句适用说明，不再使用 30px+ 长句喊话标题；56px 级大字只属于首页 Hero 和盘面干支域字。
8. **Admin 不换肤**。运营后台长时间高密度读表，保持中性灰白基底（§12），只共享控件与状态语义。
9. **暗色模式本轮不做**。`color-scheme: light` 维持；纸墨语言的墨夜变体记入待办，待五术通过用户验收后单独立项。理由：111 条路由的状态面在本轮已翻倍返工，暗色会再翻一倍验收面。TabTin 类参考站的暗底/科技蓝不得作为暗色预演贴进 C 端。
10. **完成度不等于换皮（2026-08-22）**。页面「看起来做完了」来自：一屏一事的章节闭合、标题与正文的字阶落差、真盘装进册页框、动效入场后停止、每屏一个主 CTA。权威分析与取舍表见 `docs/redesign/2026-08-22-tabtin-direction.md`。第一落地是首页与已上预览的八字/梅花结果页，不立刻重开紫微宫环与大六壬天地盘。

### 2.3 五术盘面身份（共同骨架，不同盘形）

五术共用 §7 的三态骨架与工作条，靠各自盘面结构建立身份：

| 术 | 盘面主组件 | 形 |
|---|---|---|
| 八字 | `BaziPillarMatrix` | 列=年月日时、行=事实类别的按柱矩阵 + 大字四柱行 |
| 紫微 | `ZiweiPalaceBoard` | 4×4 十二宫环盘，中宫放命主资料与命身摘要 |
| 六爻 | `LiuyaoLineTower` | 自下而上六爻塔：爻画/动静 + 纳甲 + 六亲 + 六神 + 世应列 |
| 梅花 | `MeihuaTriad` | 本卦-互卦-变卦三卦横列 + 体用关系条 |
| 大六壬 | `DaliurenBoard` | 天地盘环 + 四课列 + 三传阶梯 |

空盘剪影 = 同一组件的 `silhouette` 态：结构线与格位齐全、内容格空置，配一句「提交后由服务端生成，可核验」。剪影不放任何假干支/假星曜/假爻（§14 数据真实性）。

### 2.4 印章术标

每术一枚 24–32px 朱砂印章式术标（八字/紫微/六爻/梅花/六壬单字或双字印），用于产品页头、盘面角落与导航当前项。属于「审核过的领域图形」（§13 图标规则），`aria-hidden="true"`，旁边必有文字术名。印章是唯一允许的品牌装饰图形；不做星空、八卦壁纸、水墨大图进产品页。

### 2.5 状态文案基线

用户可见状态文案（正式页与 `/_ui-lab` 共用；2026-08-19 裁定延续）：

- unauthorized：需要登录才能看这份结果
- unavailable：结果服务暂时不可用，不会展示未确认内容
- empty：还没有可展示的盘面
- error：读取失败，请重试

禁止「待接入」当标题；不优先展示英文 `ApiError.message`；页面可见文案不使用长破折号作装饰。

## 3. 设计 Token

语义 Token 是组件的唯一颜色入口。业务组件不得散落硬编码品牌色。以下替换 `ui/tokens.css` 对应段；首页剧场用 `--paper-*` Token 保留不动（仅 `/` 装饰层引用）。

```css
:root {
  color-scheme: light;

  /* 纸墨基底 */
  --color-canvas: #f2ebdd;          /* 宣纸底 */
  --color-surface: #fbf7ec;         /* 帛面：卡、面板、表单 */
  --color-surface-subtle: #f6f0e2;  /* 纸面二级 */
  --color-surface-muted: #eae2cf;   /* 纸面三级、禁用底 */
  --color-surface-inverse: #221b12; /* 墨面 */

  --color-text: #221b12;            /* 墨 */
  --color-text-secondary: #5a4e3e;  /* 淡墨 */
  --color-text-muted: #857763;      /* 灰墨：仅 ≥13px 辅助信息 */
  --color-text-inverse: #fbf7ec;

  --color-border: rgb(34 27 18 / 14%);
  --color-border-strong: rgb(34 27 18 / 26%);
  --color-overlay: rgb(34 27 18 / 44%);

  --color-action: #221b12;          /* 主操作 = 墨 */
  --color-action-hover: #3a3122;
  --color-on-action: #fbf7ec;
  --color-accent: #9a3b2f;          /* 朱砂：唯一交互强调 */
  --color-accent-hover: #7e2f25;
  --color-on-accent: #fdf9f0;
  --color-focus: #9a3b2f;           /* 焦点环 2px 朱砂 + 1px 纸底间隙 */

  /* 证据专色：金。只用于古籍命中、引文卡、可核验标记的线与底。 */
  --color-evidence: #8f6f2f;
  --color-evidence-line: rgb(143 111 47 / 45%);
  --color-evidence-wash: rgb(143 111 47 / 12%);

  /* 状态语义（纸面重校） */
  --color-info: #3f5d7a;
  --color-success: #2f6b43;
  --color-warning: #8a5f14;
  --color-danger: #a03227;
  --surface-info: #e9edf1;
  --surface-success: #e6efe7;
  --surface-warning: #f6ecd3;
  --surface-danger: #f5e4df;

  /* 五行域色：只染盘面事实（干支/星曜/爻/卦名类别），
     禁止用于按钮、链接、状态、吉凶。 */
  --element-wood: #3f7249;
  --element-fire: #ae4128;
  --element-earth: #8f6215;
  --element-metal: #64707b;
  --element-water: #2f5a78;
  --element-wood-wash: rgb(63 114 73 / 10%);
  --element-fire-wash: rgb(174 65 40 / 10%);
  --element-earth-wash: rgb(143 98 21 / 10%);
  --element-metal-wash: rgb(100 112 123 / 10%);
  --element-water-wash: rgb(47 90 120 / 10%);

  --brand-mark: var(--color-text);
  --brand-accent: var(--color-accent);
  --brand-on-accent: var(--color-on-accent);

  /* 形状：单一体系。控件 6px、卡/面板 6px、抽屉与浮层 10px、chip 用 pill。 */
  --radius-control: 6px;
  --radius-card: 6px;
  --radius-panel: 10px;
  --radius-pill: 999px;

  --shadow-card-hover: 0 1px 2px rgb(34 27 18 / 5%), 0 4px 12px rgb(34 27 18 / 6%);
  --shadow-float: 0 4px 16px rgb(34 27 18 / 10%);
  --shadow-overlay: 0 16px 48px rgb(34 27 18 / 16%);
}
```

规则：

- 纸面层级靠 canvas/surface/subtle/muted 四级和 1px 墨线分层；卡片默认无阴影，hover 允许 `--shadow-card-hover` 单层淡阴影；菜单、Dialog、Drawer 用 `--shadow-float/overlay`；
- 朱砂是唯一交互强调；金只属于证据；五行色只属于盘面事实；绿/黄/红只表示成功、警告、危险。同类意图全站只用一个色，不得页面各配一色；
- 选择、错误、锁定、成功不能只靠颜色表达（同时用图形/文字/字重）；
- 五行色与状态色的精确值允许前端在 ±8% 明度内微调以满足 AA 对比，调完回写本文；
- 除 `/` 首页装饰层外，禁止渐变、玻璃拟态、星空、八卦壁纸、发光描边和多层彩色阴影；
- Admin 在应用根覆写 canvas/surface 族为中性灰白（§12），其余 Token 共享。

## 4. 字体与排版

- 界面、表单、报告正文和 Admin 使用 `Noto Sans SC Variable`，回退系统无衬线；
- **域字（`--font-domain`，宋体系）扩大为两处专用**：① 盘面大字——干支、卦名、宫名、爻题、课传、星曜名；② 古籍引文原文块（§19.1 引文卡的原文段）。仍禁止用于公共标题、按钮、导航、表单和 Admin；
- 品牌字体保持未冻结，不擅自制作字标；
- 中文不加人为字距；数字、金额、日期、时间、订单号一律 tabular numerals；
- 字阶（在冻结字阶上追加两级域字号，其余不变）：元信息 12px，辅助 13px，标签/帮助 14px，正文 16px，强调正文 18px，卡标题 20px，章节 24px，页面标题 30px（**产品页 H1 降用 20–24px**，30px 只用于公共内容页），首页标题 40–64px；**域字 28px（盘面格内干支）、40px（四柱/卦名大字行）**；
- 展示级字号（≥24px）负字距 -0.02em ~ -0.035em、字重 600–700；中文正文与标签不做负字距；域字不做负字距（宋体字面本身紧凑）；
- 界面行高 1.4，正文 1.6，标题 1.1–1.2，长文阅读宽度 62–72ch；
- 不使用全站宋体、不使用 6rem 装饰标题，不靠极细或极浅文字制造「高级感」。

## 5. 空间、容器与断点

固定验收视口：360、768、1024、1440px。

```text
360 起：手机单列基线
768 起：完整桌面顶栏，手机底栏退出
1024 起：盘面工作台允许双栏；产品入口态允许「录入 + 空盘剪影」双栏
1280 起：复杂合参允许主区/侧区双栏
1440：最大设计验收宽度，内容不再无限拉伸
```

几何合同：

- 手机页边距 16px；768 为 24px；1024 为 32px；1440 为 40–48px；
- 公共页面主体最大宽 1120–1200px；
- 单一录入面板最大宽 496px，手机为 `calc(100% - 32px)`；
- 产品入口态 ≥1024 双栏：录入面板 456–496px 在左，空盘剪影自适应在右（剪影区最小 420px）；<1024 单列，剪影折叠为录入面板上方一条 88–120px 高的横向结构条，或整体隐藏（360 默认隐藏，避免推开首屏表单）；
- 长报告正文最大宽 720px；
- 桌面工作台盘面区目标宽 480–520px，阅读区不得小于 360px；
- 768px 工作台纵向堆叠，禁止复刻 METIS 在平板竖屏的过窄右栏；
- 复杂合参在 1024px 可继续纵向，1280px 起才根据内容拆双栏；
- 页面级永不横向滚动；真正的宽表只能在自己的容器内滚动并保留关键首列；
- 手机年月日时采用 2×2、分步选择或底部选择器，禁止硬塞四个窄控件；
- 复杂盘面可缩放、重排或使用页签，但不能靠 9px 字号和截断维持版面。

## 6. 公共壳层与导航

### 6.1 桌面端（768px 及以上）

- 64px 顶栏（纸面 + 1px 墨线底边；仅 `/` 首页允许已批准的液态玻璃顶栏处理）；
- 左侧品牌占位；中部产品入口；右侧登录/账户；
- 一级入口固定为：术数、合参、工具、每日、知识内容、账户；「合参」下含命盘合参与问事合参；
- 术数 Mega Menu 三组（本轮五术主线排前，暂缓术不撤但后置）：

```text
命：八字、紫微、七政
卦：六爻、梅花、大六壬、奇门、问事合参
相：见相
```

- 13 Provider 不得出现在导航；
- 768px 顶栏不得两行；空间不足把次要入口收进「更多」，不压缩触达尺寸；
- 菜单支持鼠标、键盘、触屏与明确关闭，焦点由 Radix 管理；
- 当前项用朱砂 + 字重 + 下划线三重表示，不只靠颜色。

### 6.2 手机端（767px 及以下）

- 56px 精简顶栏；
- 固定五项底栏：主页、术数、工具、每日、我的；
- 底栏至少 64px，加 `env(safe-area-inset-bottom)`，正文预留同等空间；
- “术数”打开全屏分组抽屉，分组与桌面一致；
- 当前项同时用图形、文字和颜色表示；
- 抽屉关闭后焦点返回触发器，系统返回键/浏览器后退行为可预测。

### 6.3 首页

首页维持已批准身份与文案（P4-007 首轮唯一勉强可用页）。2026-08-22 起允许**完成度打磨**，不允许换成 TabTin 式暗色营销站：

1. Hero：主标「十三术同根，五十五部古籍为证」+ 副标真实规模事实 + 主 CTA「开始排盘」+ 次 CTA「命盘合参」；只讲机制与规模事实，不讲准不准；
2. 机制条：确定性盘面免费、事实与解释分层、多术互证；
3. 任务入口区、合参区、辅助区照旧；活动卡按资格出现；
4. **完成度（新增）**：折页按 01–07 章节叙事（开卷 / 先做这一件 / 机制 / 命盘 / 问事 / 合参 / 开盘），一屏一事；章节题 32–40px，正文保持 16px；桌面至少一处**册页框**放入真实空盘剪影（无假干支、假卦名）；不要再增加一套与 Hero 抢戏的卡片墙。

360px 单列（册页框可藏）；768px 起三列。

#### 6.3.1 `/` 首页动效装饰层例外

宣纸基底已全站化，本条例外收窄为**动效与装饰**：水墨呼吸/漂移、符箓图形、水滴涟漪、液态玻璃顶栏只允许出现在 `/` 首页，不得推广到录入、工作台、账户或 Admin。视觉真值与既有批准记录不变（`docs/redesign/2026-08-16-homepage-liquid-glass-prototype-decision.md`）。装饰层 `aria-hidden="true"`、`pointer-events: none`、reduced-motion 静态降级（§15）。

## 7. 用户任务壳层：三态骨架

五术产品页是**同一路由上的三态状态机**，不是三个页面。`/workbench/[handle]` 只负责深链恢复并解析或重定向回所属产品路由。URL 仅使用不透明任务句柄，不放生辰、问题正文或性别。

```text
入口态（pristine / filled / validation-error / submitting）
├── 术标行：印章术标 + 术名（20–24px）+ 一句适用说明
├── 左：录入面板（§7.1）
└── 右：该术空盘剪影（§2.3；360 隐藏，768 横条，1024 起右栏）

盘面态（免费确定性盘面；loading / ready / partial-unavailable / error…）
├── 工作条：返回 / 资料摘要 / 时间层 chips / 更多（保存·导出·分享·历史）
├── 时间口径条（§22-G3；不跨界=一行平静信息，跨界=醒目标注变柱）
├── 盘面主区（§2.3 各术盘形；≥1024 左盘右读双栏，右栏 ≥360px）
├── 事实模块流（§8.1 槽位；空字段整块不渲染，§19.2）
├── 古籍命中抽屉（金线徽章，默认折叠，§19.1）
└── 深读入口（能力/Offer 驱动；无 Offer =「测试期未开放」）

报告态（深读交付；§9 ReadingDocument 阅读面）
```

### 7.1 产品录入与采集

- 录入面板置左（1024 起）或居中（<1024），首屏聚焦任务；
- 命盘录入（八字、紫微、七政）使用受测对象、历法、日期、时间、未知时辰、地点、性别和时间口径；
- 事件起局（六爻、梅花、奇门、大六壬）使用问题、场景/侧重、起卦过程或事件时空，不索取无关出生资料；
- 见相采集使用模式、独立同意、拍摄/上传、质量检查和必要问卷，不伪装成命盘表单；
- 每个产品的专有字段有自己的可见分组，不全部塞进「高级选项」；高级选项内的时间口径、晚子时策略必须带一句人话说明；
- 主要提交按钮全宽、墨底纸字、最小 48px；按钮文案用该术动词：排盘 / 起卦 / 起课；
- 字段有永久可见 Label、帮助、错误和 disabled 原因；不用 placeholder 代替 Label；
- 缺地点但可继续时弹出明确确认弹层（写清「按东经 120° 标准时排盘，不做经度修正」），不要求用户盲点两次同一按钮；
- 提交前有资料摘要（只回显用户输入与已选口径，不做客户端推导）；
- 游客能完成免费输入和盘面；保存、跨设备、深读、追问或购买时才要求登录；登录成功原地接管游客任务，不重填、不重复计算。

### 7.2 盘面态工作台

- 工作条 360–767px 首层只保留返回和当前时间层，其余动作进入「更多」菜单或底部抽屉，不换行、不横溢；
- 1024px 起盘面与阅读双栏，两栏独立滚动；低于 1024px 盘面在前、阅读在后；
- 本命/大运/流年/流月/流日等层由该术 ViewModel `time_layers[]` 声明，不做全术数硬编码；暂不可用的层可见但 disabled，写明 `unavailable_reason`；
- 手机使用粘性章节导航（章节锚点由各术 flow-spec 定义），避免无法定位的超长页面；
- 盘面、免费事实、古籍命中、深读 Offer、支付、生成、报告、追问、导出和分享是独立区域与状态；
- 任何用户可见区域都不展示 raw JSON、Provider key、snake_case、内部 ref 或调试文本。

八字全流程逐屏规格（含显示内容合同与 ALGO-GAP）见 `docs/redesign/2026-08-21-bazi-flow-spec.md`，它是其余四术 flow-spec 的样板。梅花规格见 `docs/redesign/2026-08-21-meihua-flow-spec.md`。2026-08-22 复测补钉（游客起卦同路由出盘、S3 内部键映射或隐藏、判断中文空态）见 `docs/redesign/2026-08-22-meihua-ut-rework.md`——与 §7.1「游客能完成免费输入和盘面」同条：梅花不得把刚排出的盘送进 `/account/history` 登录墙；用户可见区域遇未知 enum 选择隐藏，不把内部键当正文。

## 8. 页面族合同

### 8.1 基础术数槽位表

本轮主线为五术：八字、紫微、六爻、梅花、大六壬；七政、奇门维持现状不返工，见相暂缓。每术预制：

1. 入口与适用说明；
2. 录入、起局或采集；
3. 输入确认；
4. 免费确定性盘面；
5. 免费基础摘要；
6. 深读 Offer 或“测试期未开放”；
7. 购买确认；
8. 生成状态；
9. 结构化报告；
10. 现实核对、追问、导出和分享；
11. 未接入、暂停、无权限、失败、删除和过期状态。

每术使用独立版本化 ViewModel 和盘面组件（`bazi-chart/v1`、`ziwei-chart/v1`…）。共享工作台壳，不共享万能 `Record<string, unknown>` 展示器。

UI 必须预制的免费结果槽位如下。这里只冻结信息架构，不授权照抄参考站算法，也不表示 Runtime 已经产出全部字段。

**槽位存在不等于必须填满。**当 Runtime 只产出事实与候选时，这些槽位按 §19 呈现为证据而非结论；当某术没有判断规则时，按 §20 收缩页面形态，**不留空槽、不填模型文案**：

| 产品 | 必须预制的盘面/观察模块 |
|---|---|
| 八字 | 时间口径与四柱摘要；年月日时四柱；十神、藏干、纳音、空亡、地势、自坐、神煞；旺衰贡献；格局/喜忌/病药依据；大运、流年、关键流月；四柱图、特殊宫位、五行力量 |
| 紫微 | 十二宫盘与宫位交互；主辅杂曜；三方四正；四化；格局与命宫摘要；大限、流年和可用时间层 |
| 七政 | 星盘；命度/身度；命盘格局；功能十二宫；十一曜宿度；恩用仇难；大限小限；神煞落宫 |
| 六爻 | 问题与起卦依据；六次过程；本卦/变卦；六爻、六亲、世应、动静、旬空/月日等事实槽位；用神候选与月令旺衰证据；基础摘要与边界 |
| 梅花 | 问题与起卦方式；本卦/互卦/变卦；动爻；体卦用卦与生克关系事实；已核验关系裁定；基础摘要与适用边界 |
| 奇门 | 阴/阳遁与局式；九宫；九星、八门、八神；值符值使；用神锚点；关键格局与大局基调 |
| 大六壬 | 天地盘；四课；三传；九宗门；天将；课体候选；关键神煞与盘面综览 |
| 见相 | 版本化结构化观察；部位/区域与置信度；质量与证据充足度；用户补充信息；基础摘要。原图不作为“盘面”展示 |

### 8.2 双人合盘

八字、紫微、七政各有独立双人入口：

- 输入甲乙两个 ProfileVersion 与情侣、夫妻、亲子、合伙、职场或朋友关系；
- 桌面展示甲方、乙方、关系区；平板和手机使用甲方/乙方/关系页签；
- 免费展示双方确定性盘面和基础关系结构；
- 深读绑定双方版本、关系类型和所选术数；
- 变更任一资料或关系类型创建新任务；
- 不把双人合盘塞进单盘小卡，也不混入三术合参。

### 8.3 命盘合参

固定流程：

```text
立命 → 在八字/紫微/七政中选择至少两术 → 免费互证 → 整合深读
```

可带着具体问题进入；追问始终绑定当前已交付报告或判断卡，保持对象、版本、术数和时间范围，越界时明确引导 Recast 而不是偷偷消耗追问。

择术卡必须解释主理、参证和不入边界。结果按维度展示各术精简盘、信号、互证、分歧、缺失和证据，不把三术平均成一篇文章。

### 8.4 问事合参

固定流程：

```text
输入同一问题与时空 → 完成六爻起卦 → 生成六爻/六壬/奇门三盘 → 免费概览 → 整合深读
```

手机使用三术页签；桌面根据宽度使用三列或主盘切换。三术全部真实就绪前，正常路由诚实显示缺失能力，不用 Fixture 补齐。

### 8.5 多盘问答（已并入命盘合参）

2026-08-14 起不再是独立顶级入口；原路由保留重定向，历史任务与报告链接不失效。

### 8.6 见相（本轮暂缓，合同保留）

一个顶级入口，内部选择面相、手相、体态、综合观照；拍摄/上传、质量检查、独立同意、原图与结构化观察分层、分享永不包含原始照片的既有约束全部保留。

## 9. 报告与 ReadingDocument

报告是一张连续阅读面（720px），不用四层嵌套卡片假装层级。顺序为：

1. 资料与盘面摘要；
2. 一句话回答；
3. 主题导航；
4. 原子判断卡；
5. 依据抽屉（金线，§19.1 同款证据语言）；
6. 适用边界；
7. 逐条现实核对；
8. 资料纠正；
9. 追问；
10. 导出、分享和版本信息。

模型只产出 `ReadingDocumentV1` 允许的短判断；栏目、盘面、证据正文、免责声明和按钮由服务端 PresentationContract 与前端 renderer 组装。Accepted 历史版本不可覆盖。

八字事业深读 P0 文本红线：每个已接纳 block 的 `text` 必须逐字等于该 block 直接引用的一个 `fact.display_text`、`finding.public_text` 或 `limit.public_text`。不能拼接、改写、润色、截取 evidence excerpt，不能把古籍引文当模型正文来源；不同 block 不得复用同一直接来源，也不得用相同文本凑段数。缺少足够数量、可逐字复制且互不重复的公开直接来源时，深读正文必须停在不可交付状态；其中 finding 来源必须是 exact evidence 闭合的 Runtime Claim Unit。

事实核对与资料纠正必须分开。核对只追加 VerificationEvent，不进入模型或修改权重；资料纠正展示差异并创建新 ProfileVersion/盘面/报告。报告末尾整体反馈分别询问清晰度、帮助程度和是否解决问题；不替代逐条核对，也不能未经另行授权把正文、照片或身份资料用于训练。

## 10. 账户与身份页面

账户区结构：`/account` 为消费 App 式「我的」页——顶部身份卡，宫格入口（受测人档案、推演历史、订单与权益、通知、账户设置、邀请有礼），下方最近交付与待处理事项；移动优先，桌面同构加宽。子页固定为：受测人档案；推演历史（按 ReadingRoot 和版本组织）；订单与权益；通知；账户设置；邀请有礼（仅活动或历史存在时显示）。

报告属于推演历史，不另建重复报告库。第一版不做积分中心、余额钱包、会员等级或自动续费页面。

全局登录弹层为主，另有可深链的 `/auth/*` 页面。注册按“OTP 核验 → 设置密码 → 同意政策版本”推进；默认密码登录，同时提供 OTP 快捷登录和找回密码。微信/Apple 只预制状态，适配器完成前不展示可用按钮。

用户自己的姓名、手机号、邮箱和业务资料完整显示。公开邀请页只显示公开昵称/头像或“一位好友”。密码、验证码、Cookie、`state_token`、API Key 和其他系统秘密永不进入 UI。

## 11. 商业、交付与邀请

商业 UI 必须预制：

```text
no-offer / login-required / purchase-confirm / payment-pending
payment-success / payment-failed / payment-expired / processing
delivered / delayed / recoverable-failure / refund / reversed
cross-device-recovery
```

无真实 Offer 时显示“测试期未开放”，不伪造商品、价格、付款成功或权益。客户端回跳只显示“确认中”，服务端支付事实到达后才改变权益和订单状态。

八字深读路径固定为：登录用户创建任务 → `payment_required` → 客户端只用 `reading_version_id` 请求 server-owned checkout → 服务端解析唯一启用的 `bazi-deep` Offer 和本任务 Purchase Target → 只有已确认且目标/商品族/归属都匹配的 `payment_id` 才能绑定履约 → `queued/processing/delivered`。Fake gateway 显示不可用；没有安全 Provider 回调前，页面不得展示真实支付成功或开放线上购买。

邀请活动不占全局一级导航；`/invite/{code}` 全状态、个人中心归因与奖励、首页临时卡、后台归口、退款限制显式勾选、链接不含个人信息等既有条款全部保留。

## 12. Admin 设计合同

Admin 是独立 `admin/` 应用和 Staff Session。**Admin 在应用根覆写基底 Token 为中性灰白（近似原 `#fafafa/#ffffff` 体系），不使用宣纸底与朱砂强调**；控件形状、状态语义、间距体系与 C 端共享。

一级六组：总览；用户与数据；产品与内容；排盘与解读；商业运营；系统与审计。

布局：

- 56–64px 顶栏；1024px 起 240px 固定侧栏；低于 1024px 抽屉；
- 桌面高密度，操作目标仍至少 44px；
- 列表包含标题、筛选、表格/摘要行、分页、批量选择、详情抽屉和全部空/错/无权限状态；
- 写操作使用明确确认弹层，显示影响对象、原因和审计结果；
- 表格移动端优先转换为摘要行，不能让整个页面横滚。

有权限员工可直接查看完整业务数据；数据库静态存储继续加密。密码哈希、验证码、Cookie、Token、API Key、数据库口令、模型 Prompt 与系统秘密永不展示。Staff 固定 support、finance、ops、superadmin 四类角色；隐藏按钮不是权限控制，每个无权限页面和写操作都有服务端拒绝与清楚的只读状态。

## 13. 通用组件合同

- `Button`：Primary（墨底纸字）、Secondary（纸面墨字 1px 墨线）、Ghost、Destructive、Icon；最小 44px，主提交 48px；同一页面同一意图只允许一个主按钮文案；
- `Field`：可见 Label、Hint、Error、Disabled reason、Required marker；输入控件 48px；Label 在上、错误在下，不用 placeholder 当 Label；
- `SegmentedControl`：历法、性别、模式、时间层，完整键盘行为；
- `Card`：普通、选择、摘要三类，默认无阴影，1px 墨线；
- `Dialog/Drawer/Popover/MegaMenu`：使用 Radix 管理焦点；
- `Tabs`：工作台、双人合盘和移动盘面，支持方向键/Home/End；
- `StatusPanel`：loading、empty、error、processing、success、unavailable、unauthorized、locked（文案基线见 §2.5）；
- `Table`：排序、筛选、选择、分页和详情；宽表只在内部横滚；
- 盘面组件族（§2.3）：`BaziPillarMatrix` / `ZiweiPalaceBoard` / `LiuyaoLineTower` / `MeihuaTriad` / `DaliurenBoard`，每个都有 `silhouette`（空盘剪影）、`loading`（结构骨架）、`ready` 三形态；视觉盘面必须有同等信息的语义列表或表格替代；
- 共享卦象组件族（六爻/梅花共用，只做一套）：`LineGlyph`（单爻画，阳实横/阴断横 + 动爻标记）、`HexagramFigure`（六爻画自下而上堆叠，含 silhouette 与输入直显态）、`TrigramGlyph`（三爻小卦画 + 卦名，自绘笔画不用 Unicode 卦符）、`HexagramHeader`（卦名 + 上下卦组成行）。复用边界：共享层只管画卦；纳甲/六亲/六神/世应/伏神单元格是 `LiuyaoLineTower` 专属，体/用标注是 `MeihuaTriad` 专属。清单权威见 docs/redesign/2026-08-21-liuyao-flow-spec.md「共享卦象组件族」；
- `EvidenceBadge` / `EvidenceDrawer`：金线证据徽章与引文抽屉（§19.1、§21.3）；
- `SealMark`：印章术标（§2.4），装饰性、`aria-hidden`；
- `Toast`：只做短反馈；付款、生成、隐私、删除和重要错误不能只靠 Toast；
- 表单使用 React Hook Form + Zod；复杂交互使用 Radix；图标只用 Lucide 功能图标（既有依赖，1.75px 统一描边）或审核过的领域图形；
- 动效只用 CSS 或 `motion/react`，不得混入第二套 Primitive 或动效系统。

## 14. 状态与 Fixture 合同

正常产品路由：已接 Runtime 时显示真实事实；未接时显示“适配中/暂不可用”；永不加载 Fixture。空盘剪影只含结构线与空格位，不含任何示例干支、星曜、爻象。

`/_ui-lab`：

- 只在开发和测试环境开放，生产返回 404；
- 顶部永久显示“UI 演示数据”；
- Fixture 使用正式版本化 ViewModel；
- 可按页面、状态、视口筛选；
- 至少预制：

```text
pristine / filled / validation-error / submitting / loading / empty
ready / free-summary / locked / need-login / need-input / queued
preparing / generating / validating / accepted / delayed / failed
follow-up / unauthorized / forbidden / unavailable / maintenance
payment-confirming / payment-success / payment-failed / refund / reversed
invite-planned / invite-active / invite-paused / invite-full
invite-ended / invite-invalid / invite-self
```

UI 完成度与算法接入度分开记录；一个不能替代另一个。

## 15. 动效与完成度

权威时序、采用/改编/拒绝表与第一落地页见 `docs/redesign/2026-08-22-tabtin-direction.md`。本节是合同摘要。动效系统仍只有 CSS 与 `motion/react`。

### 15.1 完成度（「看起来做完了」）

一页同时满足才算做完：章节闭合（能数出第几章、一屏一事）、标题与正文至少差两级字阶、主盘在册页框里、入场 ≤520ms 后静止、每屏一个主下一步。半成品的相反面是等权模块墙、同号标题、真数据躺在通用卡片里、没有抵达动作。

册页框 = 1px 墨线 + 现有纸影 + 6–10px 半径 + 术印/术名；内嵌真实盘面组件或其 silhouette。禁止 macOS 暗色窗、红绿灯、科技蓝描边、插画假盘。

### 15.2 时长与曲线

| 用途 | 时长 | 说明 |
|---|---|---|
| 反馈 | 120–180ms（token 160ms） | chip、焦点、边框 |
| 浮层 | 180–240ms（token 220ms） | 抽屉、弹出、时间层交叉淡入 |
| 路由壳 | ≤280ms | 不动画布局尺寸 |
| 章节揭示 | 480ms | 新增；仅 opacity + `translateY(12–16px)`，`once` |
| stagger | 60ms | Hero 或章内最多 4 子项 |

曲线统一 `--ease-out`：`cubic-bezier(0.16, 1, 0.3, 1)`。只动画 opacity 与 transform。

### 15.3 谁动、谁故意不动

- **动**：章节块与册页框整框入场；时间层叠加列交叉淡入；抽屉。
- **不动**：导航壳；落定后的标题与正文；干支/爻画/卦画/体用印；空盘剪影；证据金线。
- 四柱与三卦**禁止逐格 stagger**（老虎机感）。时间层禁止翻面，只做 220ms 透明度。
- 骨架屏必须匹配最终盘面轮廓（矩阵 / 宫环 / 爻塔 / 三卦），不用通用圆形 spinner 冒充盘。
- `/` 装饰层例外维持既有批准（长周期、低频率、只动 transform/opacity、`aria-hidden`、`pointer-events: none`、reduced-motion 静态降级）。不把循环装饰扩到录入、工作台、账户、Admin。

### 15.4 禁令（旧禁令仍在，2026-08-22 再钉）

- 盘面禁止：卡片翻转、悬停放大（scale>1.02）、常驻循环、星环、漂浮字、发光、弹跳 CTA。
- 全站禁止：滚动劫持、视差舞台、娱乐性支付/隐私/错误/删除动效。
- 禁止把 TabTin 的暗底、霓虹、科技蓝贴进盘面或公共页身份。

### 15.5 `prefers-reduced-motion`

关闭非必要动画，不丢信息、不延迟操作。章节揭示与入场直接终态；首页装饰层关闭；数字跳动直接显示终值；焦点环与 `aria-live` 不依赖动画。

## 16. 可访问性与信任

目标为 WCAG 2.2 AA：

- 所有操作目标至少 44×44px；
- 键盘可完成导航、表单、菜单、抽屉、盘面切换、购买和后台操作；
- 焦点环至少 2px（朱砂 + 1px 纸底间隙），不能被 sticky 区域遮挡；
- 表单有真实 Label、就近错误、顶部错误摘要和首错聚焦；
- 状态变化使用 `aria-live`，错误使用 `role=alert`；
- 复杂盘面有语义列表/表格和正确阅读顺序；
- 200% 缩放不丢任务，400% 可按单列重排；
- 相机拒绝、上传失败、删除和撤回同意都有键盘替代；
- 每页有 Skip Link、唯一 `h1` 和可预测标题；
- 五行色、朱砂、金线承载的信息必须同时有非颜色通道（文字、图形、字重）；
- 用户数据、AI 标识、权益范围、保存期限和不可用原因直说，不用神秘化文案掩盖。

## 17. 明确禁止

- 在当前产品 UI 中使用 FateRadar 名称、旧域名、旧 Logo、墨绿金旧皮肤或东方编辑档案文案；
- 把 13 Provider 做成 13 个产品页面或三术合参选项；
- 浏览器排盘或移植参考站算法（输入页的民用时辰提示等 UI 提示必须带口径边界，见 flow-spec）；
- 展示 raw JSON、Provider payload、snake_case、内部引用、Prompt 或 `state_token`；
- 未接能力展示假盘、假结论、假订单、假支付或假权益；空盘剪影不得含示例数据；
- 把出生资料、问题正文或照片地址放进 URL；
- 360px 硬塞四个日期时间控件；
- 768px 强制 520px 盘面双栏；
- 靠超小字、截断或页面横滚维持盘面；
- 金色用于证据以外的任何元素；五行色用于按钮、链接、状态或吉凶；朱砂以外新增交互强调色；
- 水墨动效、符箓、液态玻璃顶栏出现在 `/` 以外的路由；
- 把 TabTin（或同类）暗底、霓虹、科技蓝贴进 C 端身份或盘面；盘面卡片翻转、悬停放大、常驻循环装饰；
- 产品页使用 30px+ 长句喊话 H1、编号承诺清单（「提交后你会依次拿到 01/02/03…」式文档块）、面向用户的接入状态框；
- 页面可见文案使用长破折号作装饰、使用「待接入」当标题、优先展示英文 `ApiError.message`；
- 用多层嵌套卡片替代信息层级；
- 以前端隐藏 DOM 代替权益或 RBAC；
- 用 DOM 存在、CSS 正则、单元测试或 checklist 勾选宣称 UI 完成；
- **合成无来源的评分、档位或结论**：当 ViewModel 的 `status` 为 `evidence_only` 或 `predicate_matched_not_verdict`、`hard_verdict` 为 `None` 时，不得加权求和成总分，不得渲染强弱/吉凶档位条，不得输出「偏弱」「偏强」这类标签，不得提供让用户自选档位的「体感校准」控件。详见 §19.2 与 §22 的 G2；
- **虚构、润色或改写古籍原文与出处**：原文、书名、行号锚点只能原样透传。详见 §19.1 与 §22 的 G1。

## 18. 真实浏览器验收

每个公共页、用户页、产品流程和 Admin 页必须在 360、768、1024、1440 真实浏览器逐路点击。每条证据至少记录 route、state、viewport、Git SHA、截图/轨迹路径、测试结果和审阅日期。

每个路由验证：

- 首次加载与主要入口可发现；
- 正常输入、校验、提交和返回；
- loading、empty、error、unauthorized、unavailable；
- 盘面重排、文字可读、无页面级横向溢出；
- 键盘顺序、焦点、菜单、抽屉和弹层；
- sticky 不遮挡内容或焦点；
- reduced motion；
- 与参考同视口并排检查页面比例与信息密度；
- 正常路由无 Fixture、raw JSON 和旧品牌残留。

双栏工作台必须实测右侧阅读区不小于 360px。

本节是**通用质量门**。此外还须逐项通过 §22 的**差异化门 G1–G5**——那是「是否超过参考站」的判据，不通过即不算完成。

自动化和截图是证据，用户亲自浏览批准才是 `USER_ACCEPTED`。

---

## 19. 证据层与断法边界呈现合同

§8.1 冻结了「格局/喜忌/病药依据」「旺衰贡献」等**槽位**。本节规定 Runtime 只产出事实与候选时，
这些槽位如何呈现。数据字段以 `backend/app/charts/contracts.py` 与
`web/src/view-models/registry.ts` 为准。

### 19.1 古籍命中卡

数据来源：各术 ViewModel 的 `source_conditioned_patterns`。八字对应 `BaziSourcePattern`，
字段为 `rule_id` / `local_rule_id` / `title` / `source_pack` / `source_anchor` /
`status` / `fact_paths` / `predicate_audit`，其中 `status` 是类型级常量
`predicate_matched_not_verdict`。

每条渲染为一卡，三部分缺一不可：

1. **原文** —— 原样透传，不润色、不改标点、不节选到改变语义（域字宋体渲染，§4）；
2. **为什么适用于这张盘** —— `predicate_audit` 的可读化；
3. **可回溯出处** —— 书名与行号锚点。

约束：

- 文案只能表达「此条适用」「条件成立」；**不得**写「所以你…」「主…」「宜/忌…」；
- `predicate_audit` 形如 `/day_master/stem:eq:甲`，可读化映射表必须是**显式常量**，
  不得用模型或启发式猜测；未覆盖的审计串原样显示；
- `fact_paths` 是内部引用，§17 禁止展示内部引用与 snake_case，只放开发者视图或
  `title` 属性，不得进正文；
- 交互定位是**默认折叠的抽屉**，收起态只显示计数（如「命中古法 4 条 · 可核验」）。
  它是护城河，但对多数用户是噪音、对怀疑者才有价值，不占主视觉。视觉标识用证据金（§3），
  这是金色在产品中的唯一用途。

八字付费深读可消费的 Runtime Claim Unit 只能是带 `public_text` 的直接投影，并且必须携带 exact fact/evidence refs。当前源码枚举的三个单元是月令状态、子平格局入口和调候候选次序；展示时保留“未裁定”，不把它们染成吉凶、强弱或唯一用神。隐藏签名 V53 Release 尚未重建时，正常结果页不能假定这些单元存在。

### 19.2 候选与未裁定边界

候选层在类型上就不是结论。八字 `BaziStrengthEvidence` 为
`status: Literal["evidence_only"]`、`hard_verdict: None`，提供 `seasonal_state`
（旺/相/休/囚/死，带 `seasonal_state_source_rule_id`）、`same_element_occurrences`、
`resource_element` + `resource_occurrences`、`all_element_occurrences`、`boundary`。

**这些是计数与一个有来源的状态，不是分数。**

- §8.1 的「旺衰贡献」槽位渲染为**证据清单**：分别列出支持强与支持弱的因素；
- `boundary` 声明的未裁定边界必须原样显示，不折叠隐藏；
- `structure`、`following_and_transformation` 是候选，文案必须带候选语义
  （「候选」「待裁定」），不得渲染成已确定的格局名；
- `reasoning_tools` 键名不固定，按键遍历，**未知键保留原值原样显示**，不猜含义、不丢弃；
- 字段普遍可空（八字 `BaziCoreFacts` 20 个字段全部 `| None`）。缺字段时该区块整体不渲染，
  不显示「暂无数据」占位条，不用零值或空串顶替。

### 19.3 术语可读性：定义可以，吉凶不行

用户看不懂术语是真问题，但翻译不得越界。判据是**只解释这个词指什么，不解释它意味着什么**。

| 术语 | 允许 | 禁止 |
|---|---|---|
| 旬空 | 「日柱所属那一旬中缺位的两个地支」 | 「旬空主虚耗」 |
| 十二长生 | 「该柱天干在本柱地支上的生旺状态位」 | 「墓地主困顿」 |
| 冲 | 「两地支相对，属六冲关系」 | 「冲主动荡不安」 |

吉凶含义属断法层，须有已核验来源规则，且必须走 §19.1 的引文卡形式。

关系类型不得自动染成吉凶色（不要把「冲」渲染成红色警示），用中性色区分类别。

---

## 20. 能力分层合同

§8.1 的槽位表对各术一视同仁。本节规定 Runtime 能力不足时页面如何收缩，
以满足 §17「未接能力不得展示假盘假结论」。

### 20.1 三档

| 档 | 条件 | 页面形态 |
|---|---|---|
| **A · 三层齐全** | 有已激活判断规则 | 盘面 + 候选/证据 + 古籍命中抽屉 + 深读 Offer |
| **B · 事实完整** | 盘面事实完整但判断规则为 0 | 盘面 + 明示边界；**不出现断法区块**，不留空槽 |
| **C · 未接入** | Provider 未接或字段缺失 | 「适配中/暂不可用」，永不加载 Fixture（§14） |

「判断规则」的机器定义唯一：已签名 release 的 `references/index/evidence-rules.jsonl` 中
`runtime_active == true` **且** `evidence_role == "issue_specific_judgment_rule"` 的记录条数。
`methodology_rule`、`casting_rule`、`timing_rule`、`imagery_correspondence`、`terminology_only`
都不是判断规则，不能用来把一术抬进 A 档。分档实现必须现算这个统计，不得在前端硬编码结果表。

2026-08-18 实测 `.runtime/v53-time-check-release`（1328 条规则 / 192 条 `runtime_active`），
按 `system` 字段统计判断规则：

| system | 术数 | 判断规则 | 档 |
|---|---|---:|---|
| `luming-nayin` | 禄命纳音 | 56 | A |
| `qimen` | 奇门 | 40 | A |
| `bazi` | 八字 | 19 | A |
| `taiyi` | 太乙 | 15 | A |
| `liuren` | 大六壬 | 5 | A |
| `divination` | 六爻 / 梅花 | 5 | 按 rule_id 前缀拆分，见下 |
| `fengshui` | 风水 | 1 | A |
| `selection` | 择日 | 1 | A |
| `ziwei` | 紫微 | 0 | B |
| `xingming` | 七政四余 | 0 | B |
| `physiognomy` | 见相 | 0 | B |

复核命令：

```bash
python3 - <<'PY'
import json, collections
p = ".runtime/v53-time-check-release/references/index/evidence-rules.jsonl"
c = collections.Counter()
for line in open(p, encoding="utf-8"):
    if not line.strip():
        continue
    r = json.loads(line)
    if r.get("runtime_active") and r.get("evidence_role") == "issue_specific_judgment_rule":
        c[r.get("system")] += 1
print(sorted(c.items(), key=lambda x: -x[1]))
PY
```

`divination` 包同时覆盖六爻与梅花，分档实现必须按 `rule_id` 前缀拆开，不得整包算给一术。
逐条查明结果：六爻 2 条（`huangjin-ce#HJC-R009`、`zengshan-buyi#ZR-04-04`），
梅花 3 条（`meihua-yishu#MR-04-01/02/04`）。

**2026-08-21 用户裁决**：六爻与梅花按上述机器定义进档（各有已激活判断规则，不再维持 B 档特例）；
`_tier_for` 的 B 档特例移除由后端在办单 T-0821-CAP-1 执行。页面形态随档位变化：两术开放断法区块，
但只渲染各自已核验规则实际覆盖的内容（六爻的用神角色与月令旺衰带、梅花的体用关系裁定），
不得因“已进 A 档”而放开无来源断语。

归档随 Runtime 能力变化，改动须回写 `docs/CHECKLIST.md`。

### 20.2 B 档不是残缺页

B 档是**只做前两层的完整页**：

1. 盘面区块按 §8.1 槽位表做满（紫微、七政的可寻址事实密度足够支撑）；
2. 断法/深读区块**整块不渲染**，不显示占位卡、不显示「敬请期待」；
3. 页面给一条边界说明：本术当前只提供确定性盘面与事实，不提供断语；
4. **不得因为页面「看起来短」而用模型文案补内容。**

---

## 21. 交互质量合同

§13 冻结组件、§15 冻结动效时长与禁令。本节规定信息密集盘面页的交互模式——
这是本产品相对参考站的可感知优势所在，也是「好看」之外真正决定体验的部分。

### 21.1 盘面联动（最高优先级）

盘面事实数以百计，堆成表格就是死数据。要求同一事实的所有出现位置**联动高亮**：

- 聚焦或悬停某一天干/地支/星曜时，其藏干、十神、关系图连线、五行统计贡献、
  相关宫位/柱位**同时**进入高亮态；
- 高亮只用边框、底色与字重表达，不用位移、缩放或发光（§15）；
- 必须有非悬停通路：键盘聚焦与点击锁定等效，触屏以点击锁定；
- 锁定态可再次点击解除，`Esc` 解除全部锁定。

这是纯事实联动，零断语，把静态密度变成可探索结构。

### 21.2 时间层切换的连续性

本命/大运/流年/流月/流日切换时：

- 盘面容器不卸载、不整块重绘、不改变页面几何；
- 只对变化的标记与位置做 120–180ms 的 opacity/transform 过渡；
- 切换过程中已有内容保持可读，不出现骨架屏闪烁；
- 当前时间层必须在盘面与导航两处同时可辨。

### 21.3 证据的渐进披露

三级，不允许把引文一次性堆在页面底部：

1. 盘面元素旁一个克制的来源标记（有涉及规则时才出现，用证据金）；
2. 悬停或聚焦显示「有 N 条古法涉及此处」；
3. 点击展开 §19.1 的引文卡。

### 21.4 盘面键盘可遍历

四柱、十二宫、九宫、四课三传、六爻塔等结构必须可用方向键在格位间移动，
`Home`/`End` 跳首尾，焦点可见且不被 sticky 遮挡。§13 要求的语义列表/表格替代仍然保留。

### 21.5 事实对比（不是评分对比）

允许把两个时间层并排比较**事实差异**（如本命宫位 vs 当前大运宫位）。
差异只做中性标注，不得给出优劣、吉凶或分数。

### 21.6 本页明确不做

以下在信息密集页面是干扰，一律禁止：滚动触发动画、视差、数字滚动计数、
卡片翻转、装饰性进度条、悬停放大、常驻动效。§15 的既有禁令继续有效。

---

## 22. 差异化验收：六道门

§18 是通用质量门。本节是**「是否超过参考站」的判据**。要求全部可判定——
「看起来更好」不是判据。G1–G5 逐项附参考站实测反例；G6 即 §18。

### G1 · 引文可核验率 100%

页面出现的每一条古籍引用，抽取后经 `scripts/verify_citation.py` 校验必须全部返回
`verified_exact`。核验链分四步，前 3 步与第 4 步不能互相冒充：

1. 页面 `excerpt` 逐字等于其 `evidence_ref` 指向的签名发行记录 `verbatim_quote`；
2. `sha256(verbatim_quote.encode("utf-8"))` 等于记录的 `verbatim_quote_sha256`；
3. 记录的 `path + sha256` 锁定语料文件版本，`anchor` 与页面 `locator` 一致；
4. 独立全文语料中，该原文确实位于该书该行。

第 1–3 步只依赖签名 release，以 ref 绑定的 `release-bound` 模式判定；它不读取全文，返回
`verified_release_bound` 只能说明发行记录绑定闭合，不能单独通过 G1：

```bash
python3 -B scripts/verify_citation.py \
  --mode release-bound \
  --release-root .runtime/v53-time-check-release \
  --file <页面-evidence.json> \
  --citations-file <引文清单>.txt                         # 退出码必须为 0
```

第 4 步必须读取独立授权的 `mingli-master` 全文语料。默认安装位置为
`~/.codex/skills/mingli-master`；也可用 `--root` 指定。可直接复核：

```bash
PYTHONDONTWRITEBYTECODE=1 \
PYTHONPATH=.runtime/backups/2026-08-18-g1-resign/runtime-extras \
~/.local/share/mingli-master/venv/bin/python -B scripts/verify_citation.py \
  --root ~/.codex/skills/mingli-master \
  --file <引文清单>.txt                                  # 退出码必须为 0
```

签名 release 不内置 `references/fulltext`。C+B 路线是再分发授权边界，不是体积优化；取得明确授权前，全文不得进入 release。缺少独立语料时必须 fail closed。G1 的 100% 与 `verified_exact` 要求不变：页面全部引文须先通过第 1–3 步，并在已安装的独立全文上全部通过第 4 步。

参考站实测反例：青囊八字页标注「偏财源活，最宜食伤生扶；忌比劫劫夺。」出自
《子平真诠·论财》，在该书全文库中 `not_found`，四个关键词组零命中，
全库最高 3-gram 包含率仅 8%；且原文立场相反（「財旺無劫而透傷，反爲不利」）。

**参考站结构性做不到这一门**——它们没有全文库与行号锚点。

### G2 · 零合成断语

自动化断言：页面无无来源总分数值、无强弱/吉凶档位条、无「偏弱」「偏强」结论标签、
无「体感校准」档位控件；`status` 为 `evidence_only` / `predicate_matched_not_verdict`
时对应区块不得出现结论式措辞。

参考站实测反例：青囊「日主气数」给出得令 −16 / 得地 −19 / 综合分 −38 / 档位「偏弱」，
标注「基于《滴天髓》四层考核」，而《滴天髓阐微》全文 11493 行中既无「四层考核」这一说法，
也无任何数值系数。

**这一门是自我约束，不是能力。**验收时发现任何合成评分，无论多好看，一律打回。

### G3 · 时间口径完全透明

结果页必须可见：时间策略 ID、经度修正量、均时差修正量、修正后有效时刻、
边界状态、晚子时流派。修正跨越时辰或日界时必须标出「该修正改变了 X 柱」。

参考站实测反例：同一输入 `1985-03-02 00:00 北京`，本产品得 乙丑/戊寅/**己亥**/**甲子**，
青囊得 乙丑/戊寅/**庚子**/**丙子**。两家真太阳时都算出 23:33，但日柱不同——
日柱不同即整张盘不同。两种晚子时流派都合法，问题是参考站同屏显示「真太阳时 23:33」
与次日日柱却不标注流派。

默认态要诚实：多数盘的修正不跨界，此时是一条平静的信息行，不得硬做成戏剧性提示。

本门的“结果页”指 owner-scoped 的本人结果与历史报告。Bearer 分享页按服务端分享隐私投影渲染，不得为了满足 G3 下发或显示精确有效时刻；它可以保留不含出生时刻的策略与边界说明。

### G4 · 能力分层诚实

按 §20 三档逐 route 断言：B 档不渲染断法区块也不渲染占位卡；C 档显示
「适配中/暂不可用」且不加载 Fixture；A 档断法内容全部可追溯到已激活规则。

参考站对比：两家对所有术数一律给结论，包括规则依据无法核验的部分。

### G5 · 事实密度不低于参考站

同视口（1440 与 768）并排截图，统计可见结构化事实条目数，本产品不低于参考站。

达成方式是 §8.1 槽位做全 + §21.1 联动做对，**不得**靠超小字、截断或页面横滚（§17）。
密度统计以按柱矩阵/宫格内的结构化格计数，摊平成定义列表凑条数不算达成。

### G6 · 通用质量门

即 §18 全部要求：四视口、六态、无页面级横向溢出、右栏 ≥360px、键盘与焦点、
reduced motion、正常路由无 Fixture/raw JSON/旧品牌残留、与参考站同视口并排检查。

### 关于「好看」

视觉品质无法由文档判定，只能由用户在真实浏览器中裁决（§18）。本文能约束的是
模式、密度、层级与禁令；最终观感以用户浏览批准为准。**任何一道门不通过，
观感再好也不进入 `USER_ACCEPTED`。**

---

## 23. 2026-08-21 修订记录：废弃与保留清单

### 废弃（经用户授权）

1. 方向 C「现代 SaaS 锐感」整体审美合同：浅灰底、纯白面、蓝色唯一点缀、8px 圆角体系（原 §2、§2.1 的方向性条款）。
2. 全站蓝色强调（`--color-accent: #2563eb` 族）：交互强调改朱砂；蓝仅存于 `--color-info` 与五行水色。
3. 「宣纸/暖色只限 `/` 首页与录入页」的例外条款（原 §6.3.1 部分）：宣纸基底升级为全站 C 端基线；首页例外收窄为动效装饰层。
4. 原 §2.1 中「结果页主视觉」条目的表述由 §2.2-6「盘面是产品的脸」取代（可核验引文仍是差异化核心，见 §19、§22-G1，但主视觉是盘面矩阵，引文以金线徽章渐进披露）。
5. 产品页大标题模板（30px+ 长句 H1）、右栏编号承诺清单、面向用户的「已接入」状态框（§17 新禁令）。
6. `/bazi` 成功态首屏「必须同时有一条已核验引文」的旧硬性要求：改为证据金徽章常驻可见 + 渐进披露（§21.3）；引文可核验率仍受 G1 全量约束。
7. 原 §20 「六爻/梅花维持 B 档待用户确认」的暂挂条款：2026-08-21 用户已裁决进档（§20.1）。

### 保留（继续有效）

1. §19 证据层呈现、§20 能力分层（含机器定义与复核命令）、§21 交互质量、§22 六道门：全部原样保留（仅按上文更新六爻/梅花裁决与 G5 计数口径说明）。
2. 全部技术硬约束：360/768/1024/1440 四视口、页面级不横滚、右栏 ≥360px、44/48px 触达、WCAG 2.2 AA、reduced motion、状态库存（§14）、真实数据边界（正式路由禁 Fixture/假盘/raw JSON）。
3. 主操作黑（墨）底、密度跟 METIS、质感来自网格与字阶（原 §2.1 存活条款，并入 §2.1/§2.2）。
4. 状态文案基线 unauthorized/unavailable/empty/error（并入 §2.5）。
5. 字阶冻结（追加两级域字号）、`Songti SC` 域字边界（扩大至古籍引文原文块）。
6. 组件技术栈边界：Radix、React Hook Form + Zod、`motion/react` 单一动效系统、Lucide 功能图标。
7. 商业状态机、账户结构、Admin 六组、邀请合同、报告 P0 文本红线：未动。
8. 首页 Hero 文案、双 CTA、已批准的水墨/液态玻璃视觉真值：身份保留。2026-08-22 起允许按 §6.3-4 / §15 做章节编号、字阶对比、册页入画与动效时序，**不是**把首页改成暗色 SaaS 营销页。

### 2026-08-22 补钉仍生效的旧禁令

- 暗色整站 / 墨夜变体仍待办，不以 TabTin 暗底预演（§2.2-9）。
- 盘面禁娱乐动效：翻转、悬停放大、常驻循环（§15.4、§17、§21.6）。
- 金只属于证据；五行色只染事实；朱砂是唯一交互强调。
- `motion/react` 仍是唯一 JS 动效系统；不得引入第二套滚动库或 Lottie。
- 第一落地不得重开紫微大盘与大六壬天地盘来「证明完成度」。

### 待办（另行立项，不阻塞本轮）

- 暗色「墨夜」变体（§2.2-9）；
- 品牌名、Logo、品牌字体冻结；
- 术语释义常量表的来源审核（flow-spec GAP-BZ-03）；
- 紫微 / 大六壬大盘的完成度改编（等首页+八字/梅花结果页在预览上像做完了再开）。
