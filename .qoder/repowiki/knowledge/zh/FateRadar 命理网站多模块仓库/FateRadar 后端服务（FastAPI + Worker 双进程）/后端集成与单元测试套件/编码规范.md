- 通过 `importlib.import_module('app.*')` 延迟导入被测模块，避免在 fixture 阶段触发生产环境副作用。
- 编排器相关测试统一使用 `orchestrator_fakes.py` 中的 `Scripted*`、`MemoryRepository`、`FixedClock` 与 `make_job`/`make_prepare`/`make_candidate` 工厂构造确定性执行上下文。
- JSON 测试数据集中在 `fixtures/mingli/` 与 `fixtures/narrative/` 目录下，由 `load_candidate` 等工具函数加载，而非内联在测试中。
- HTTP 集成测试通过 `client` fixture 发起请求后，使用 `async with database.sessions()` 直接查询 SQLAlchemy 模型断言持久化结果。
- 测试用例命名采用 `test_<领域>_<场景>` 形式，每个异步测试函数独立断言状态机流转与副作用。
- 对外部依赖（runtime/model/guard/repository）均以可插拔接口注入，测试通过替换为脚本化 fake 来模拟成功、失败、取消等多种分支。