运行前需在 `backend/contracts/schemas/` 下提供 `mingli-command-v2.schema.json` 与 `mingli-result-v2.schema.json`，否则 `runtime_contracts._validator` 加载失败；`ReadingJob.max_attempts` 必须为 1 或 2，transport backoff 必须为正数，由 `__post_init__` 强制校验。