httpx 异步 HTTP 客户端调用 DeepSeek/DashScope OpenAI 兼容 API；stdlib `smtplib` + `ssl` 发送 OTP 邮件；`asyncio.subprocess` 以独立进程运行 Node/Python 可移植 Runtime；Pydantic `SecretStr` 管理密钥；JSON Schema 校验 NarrativeCandidate。