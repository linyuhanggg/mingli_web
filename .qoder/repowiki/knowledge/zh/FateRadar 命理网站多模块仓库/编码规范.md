- 所有跨进程 API 变更必须先更新 contracts/openapi 与 contracts/schemas，再由 tests/contract 校验各子模块实现一致性。
- 数据库 schema 变更统一通过 backend/alembic 迁移文件管理，由 make migrate 或 CI 驱动升级。
- 后端使用 ruff 做 lint、mypy 做类型检查，前端使用 npm lint/typecheck，统一通过根 make check 聚合。
- 敏感配置（支付、短信、模型密钥等）不得入库，仅通过环境变量注入，本地使用 Fake OTP/Payment/Runtime。