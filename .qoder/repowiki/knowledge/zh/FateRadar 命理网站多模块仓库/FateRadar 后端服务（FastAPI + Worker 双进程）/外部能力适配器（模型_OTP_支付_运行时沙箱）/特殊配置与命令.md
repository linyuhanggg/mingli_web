生产环境需将 `settings.model_adapter` 设为 `deepseek` 并提供 `deepseek_api_key`、`model_price_snapshot_version` 及价格微单位；Runtime 启动要求 `runtime_launcher_path`、`runtime_python_path`、`runtime_release_root`、`runtime_state_root` 均非空且 `runtime_expected_manifest_digest`/`runtime_expected_capability_shape_sha256` 已配置，否则 `RuntimeStartupGate.startup()` 抛出 `RuntimeStartupError`。