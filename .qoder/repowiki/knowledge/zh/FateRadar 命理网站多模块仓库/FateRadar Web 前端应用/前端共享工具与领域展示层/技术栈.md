纯 TypeScript + 浏览器原生 API（`fetch`、`Intl.DateTimeFormat`、`globalThis.crypto.randomUUID`、`document.cookie`），无第三方运行时依赖；CSRF 通过自定义 Cookie `mingli_csrf` 与 `X-CSRF-Token` 头配合服务端会话；幂等性通过 `Idempotency-Key` 请求头实现。