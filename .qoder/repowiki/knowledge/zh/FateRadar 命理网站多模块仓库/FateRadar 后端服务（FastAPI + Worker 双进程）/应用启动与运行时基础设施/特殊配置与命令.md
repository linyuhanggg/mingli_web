运行前需设置 `MINGLI_*` 环境变量（如 `MINGLI_DATABASE_URL`、`MINGLI_OTP_ADAPTER`、`MINGLI_TRUSTED_PROXY_CIDRS`），生产环境必须提供合法的 `content_encryption_key_b64`、`identity_hash_key` 且禁用 `fake`/`smtp` OTP 与 `real_traffic_enabled`。